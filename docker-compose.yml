version: '3.8'

services:
  app-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    networks:
      - dokploy-network
  app-fronend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    env_file:
      - .env
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
